<UserControl x:Class="LyricsEditorPro.Views.HomeUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LyricsEditorPro.Views"
             xmlns:vms="clr-namespace:LyricsEditorPro.ViewModels"
             mc:Ignorable="d" 
             MinWidth="1000" 
             MinHeight="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Menu Grid.Column="0" Grid.Row="0">
            <MenuItem Header="File"/>
        </Menu>
        <Grid Grid.Column="0" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="1" Text="{Binding LyricsText, UpdateSourceTrigger=PropertyChanged}"/>
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="Black"/>
            <!--#region Queue Panel-->
            <ListView ItemsSource="{Binding Tracks}" Grid.Column="2" Grid.Row="1">
                <ListView.ItemTemplate>
                    <DataTemplate x:Name="TripleLineDataTemplate">
                        <StackPanel Height="84" AutomationProperties.Name="{Binding Name}">
                            <TextBlock Text="{Binding Name}" Margin="0,4,0,0"/>
                            <TextBlock Text="{Binding Artists}" Opacity=".8" Margin="0,4,0,0"/>
                            <TextBlock Text="{Binding Album}" Opacity=".6" Margin="0,4,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <local:PlayerUC DataContext="{Binding PlayerVM}" Grid.Column="0" Grid.Row="2" 
                        PlayerContent="{Binding DataContext.CurrentTrack,
                     RelativeSource={RelativeSource AncestorType=local:HomeUC}}"/>
    </Grid>
    <!--
     <StackPanel VerticalAlignment = "Center" >
        <TextBox Text="Welcome" HorizontalContentAlignment="Center" 
FontSize="30" FontStyle="Italic" FontWeight="Bold" Margin="0,0,0,100"/>
        <StackPanel Orientation = "Horizontal" HorizontalAlignment="Center">
            <Button MinHeight="130" MinWidth="130" Command="{Binding SelectFilesCommand}" Margin="22">
                <StackPanel >
                    <Label>Select Files</Label>
                    <!-_-<Image Width = "80" Source= "></Image>
                </StackPanel >
            </Button >
        </StackPanel >
        </StackPanel >

        -->
</UserControl>

    